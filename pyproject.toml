[project]
name = "github-stars-mcp"
version = "0.1.0"
description = "Minimal MCP server - GitHub repo info via OAuth"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.2.0",
    "httpx>=0.27.0",
]
